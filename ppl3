pipeline {
    agent any

    stages {
        stage('Dump') {
            steps {
                sh 'mysqldump -u myuser -pmypassword --all-databases -h 192.168.1.118 > databases.sql'
                archiveArtifacts artifacts: "databases.sql", followSymlinks: false
            }
        }
    }
}
